you can add
bash -c "echo Updates every 1 minute: > <website dir>/uptime.txt && tuptime >> <website dir>/uptime.txt && echo Last update:\n >> <website dir>/uptime.txt && date >> <website dir>/uptime.txt"
to a system service file and timer to make it represent your system's uptime
make sure to replace  <website dir> with your websites directory
